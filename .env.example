# Runtime LLM provider. Exact model should be selected/configured during evals.
LLM_PROVIDER=groq
LLM_MODEL=
GROQ_API_KEY=

# Safety/runtime defaults; final max steps should be calibrated from live eval traces.
LLM_TIMEOUT_SECONDS=30
LLM_MAX_RETRIES=2
# Short bounded backoff base (seconds) between transient-failure retries.
LLM_RETRY_BACKOFF_SECONDS=0.25
LLM_TEMPERATURE=0
# Optional provider reasoning control. GPT-OSS on Groq supports low/medium/high.
LLM_REASONING_EFFORT=
AGENT_MAX_STEPS=

# Optional path to an authorized local copy of the supplied Quest 4 Stage 1 starter kit.
QUEST4_STARTER_KIT_PATH=.vendor/place-il-quests/Quest 4/Stage 1/starter-kit

# Optional single-model pricing config for ad-hoc CLI runs. The eval runner
# overrides these from evals/candidates.json for per-candidate comparison.
LLM_INPUT_COST_PER_MILLION=
LLM_OUTPUT_COST_PER_MILLION=

[project]
name = "globalcart-resolver"
version = "0.1.0"
description = "GlobalCart Operations Resolver Agent - Place IL Quest 4 Stage 1"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7",
    "python-dotenv>=1.0",
    "openai>=1.40",
]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
